Flask-Backend/
└── app.py

What We've Done

- Created Flask API (`/generate-plan`)
- Accepts JSON input
- Returns dummy floor plan (summary + rooms)
- Supports CORS
- Connected with Node.js via HTTP POST

Run Flask app
python app.py